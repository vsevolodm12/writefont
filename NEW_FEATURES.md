# Новые функции

## 1. Кнопка "Познакомиться с создателем бота"

✅ **Уже реализовано!** Кнопка находится в главном меню и ведет на https://t.me/vsevolodmarchenko

## 2. Копирование шаблонов на сервер

Для копирования шаблонов на сервер используйте скрипт:

```bash
./scripts/copy_templates_to_server.sh
```

Скрипт покажет список файлов и команды для копирования на удаленный сервер:

```bash
# Через scp
scp -r templates/ user@server:/path/to/consp_bot/

# Или через rsync
rsync -avz templates/ user@server:/path/to/consp_bot/templates/
```

## 3. Шрифт создателя

### Настройка

1. Создайте папку `fonts/creator/` в корне проекта
2. Поместите ваш .ttf файл шрифта в эту папку
3. Или поместите шрифт напрямую в папку `fonts/` (будет использован первый найденный .ttf файл)

### Использование

Пользователи смогут нажать кнопку "✨ Попробовать шрифт создателя" в меню загрузки шрифтов (`📥 Загрузить шрифты`), и шрифт будет автоматически добавлен к их аккаунту для тестирования.

Шрифт копируется для каждого пользователя отдельно с уникальным именем, чтобы не конфликтовать с другими шрифтами.

